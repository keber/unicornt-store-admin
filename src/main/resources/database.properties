# Generado por Maven al empaquetar - NO editar manualmente.
# Usa:  mvn package -P mysql    (por defecto)
#       mvn package -P postgres
db.driver=${db.driver}
db.url=${db.url}
db.user=${db.user}
db.password=${db.password}
